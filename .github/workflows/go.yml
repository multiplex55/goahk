name: Go CI

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

jobs:
  platform-sanity:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Go
        uses: actions/setup-go@v5
        with:
          go-version: 'stable'
          check-latest: true
          cache: true

      - name: Verify no tracked binary artifacts
        run: ./build/check-no-source-binaries.sh

      - name: Build-tag sanity check (non-Windows)
        run: go test -v ./internal/runtime -run TestNewWindowsListenerOnNonWindowsReturnsExplicitError

  build-and-test-windows:
    runs-on: windows-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Go
        uses: actions/setup-go@v5
        with:
          go-version: 'stable'
          check-latest: true
          cache: true

      - name: Verify Go version
        run: go version

      - name: Install dependencies
        run: go mod download

      - name: Build application
        run: go build -v ./...

      - name: Run tests
        run: go test -v ./...
