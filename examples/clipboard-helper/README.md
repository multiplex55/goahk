# clipboard-helper

Clipboard transform helper example.

## Expected behavior

- Copy text containing `foo`.
- Press `Ctrl+Shift+V` to paste transformed text with `foo` replaced by `bar`.
- Press `Escape` to stop the app.
